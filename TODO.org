* TODO commit popup binding

In commit messages, I can quit by pressing escape. I'd like this to not be the case. To abort, I can press mod+d (alt+d), or do :bd. Implement this.

* TODO org table formatting
There's a bug in org table formatting, where, links dont get properly formatted. May be related to precedence when formatting org tables between link formatting and column formatting (w auto page wrapping).  Investigate and fix.

* TODO org table render

I'd like org tables to render better. Should have slightly different backgrounds
one row after the other. I'd also like there to be a rounded border, similar to
the style that org code blocks have.

* TODO lua formatter

I'd like an inhouse version of a formatter for lua. Can you implement one? I'd
like it to run without previously exisiting linters (i.e. entirely inhouse).

* TODO java formatter

I'd like an inhouse version of a formatter for java. Can you implement one? I'd
like it to run without previously exisiting linters (i.e. entirely inhouse).

* TODO c++ formatter

I'd like an inhouse version of a formatter for c++. Can you implement one? I'd
like it to run without previously exisiting linters (i.e. entirely inhouse).

* TODO c formatter

I'd like an inhouse version of a formatter for c. Can you implement one? I'd
like it to run without previously exisiting linters (i.e. entirely inhouse).

* TODO python formatter

I'd like an inhouse version of a formatter for python. Can you implement one? I'd
like it to run without previously exisiting linters (i.e. entirely inhouse).

* TODO r formatter

I'd like an inhouse version of a formatter for R. Can you implement one? I'd
like it to run without previously exisiting linters (i.e. entirely inhouse).


* TODO smooth scroll

I'd like smooth scrolling in org files. When going over a figure, I'd like j,k
to scroll one line at a time until the position of the top or bottom of the
figure scrolls past the center of the height of the window (i.e. where the
cursor should be emulated from (but not visible, while going over the image),
depending how on which way the user is scrolling.

* TODO error binding

I'd like a binding for going to the next error, should be [e and ]e. Once the
error has been jumped to, id like a popup to show the error message(s). Should
be able to select error in popup (ctrl+n, ctrl+p) and yank the error message to
the clipboard (y).

* TODO lsp status
I'd like org mode code blocks to give status of running lsp at the bottom of a
code block (separated by a horizontal line (from the code part of the block).

* TODO octave lsp engine
I'd like an inhouse LSP engine for octave. Can you implement one? I'd like it to run without previously exisiting linters (i.e. entirely inhouse)

* TODO java lsp engine
I'd like an inhouse LSP engine for Java. Can you implement one? I'd like it to run without previously exisiting linters (i.e. entirely inhouse)

* TODO c++ lsp engine
I'd like an inhouse LSP engine for C++. Can you implement one? I'd like it to run without previously exisiting linters (i.e. entirely inhouse)


* TODO c lsp engine
I'd like an inhouse LSP engine for C. Can you implement one? I'd like it to run without previously exisiting linters (i.e. entirely inhouse)


* TODO python lsp engine
I'd like an inhouse LSP engine for python. Can you implement one? I'd like it to run without previously exisiting linters (i.e. entirely inhouse)


* TODO R lsp engine
I'd like an inhouse LSP engine for R. Can you implement one? I'd like it to run without previously exisiting linters (i.e. entirely inhouse)



* TODO org export 
I'd like a button at the top of an org mode buffer (pane bar) for exporting the
org document. I should be able to press the button and for the export button to
have a dropdown menu with options: html, pdf, odt, markdown,
ascii, etc... When I select one of these options, it should export the org document to
that format and open it in a new pane. Can you implement this?

* DONE org insert block

I'd like a button at the top of an org mode buffer (pane bar), I'd like to be
able to insert a code block (e.g. R, python, C/C++, etc.) into the org mode buffer. Can you implement this?
  - A `</>` button now sits in an org buffer's pane header, left of the Run
    button and shown under the same conditions (a plain .org text pane, no
    viewer session on the buffer). Clicking it drops down twelve common
    languages -- Python, R, C, C++, Rust, Go, Lua, JavaScript, TypeScript,
    Java, Shell, Julia -- plus an "Other language..." row; picking one
    writes an empty `#+begin_src <lang>` / `#+end_src` pair at the cursor
    and leaves the cursor on its body line. The same insert is reachable
    without the mouse as `<leader>oi` and `:MepOrgInsertBlock` (a picker
    over every language org-babel registers, with a free-text entry for one
    it doesn't), or directly as `:MepOrgInsertBlock rust`. A blank cursor
    line becomes the block itself, any other line keeps its text and the
    block goes below it, and with the cursor already inside a block the new
    one lands after that block's `#+end_src` instead of nesting inside it.
    The insert itself is Lua (kBuiltinOrgInsertBlock, main.cpp) so any
    language tag works, listed or not; the button and its dropdown are
    DrawPane's header controls and DrawOrgInsertBlockMenu, built the same
    way the Run button's Setup menu and a notebook cell's kernel dropdown
    already were. Documented in help/org-babel.org.

* TODO link formatting
Change links in org mode to be rendered to have an underline underneath it. 

* TODO org play
Add a play button to org mode code blocks that will run the code block in the appropriate language mode.

* TODO org settings
Add a settings button to orgmode code blocks that will allow the user to set the settings for the code block (e.g. results, exports, etc.). On click, it should bring up an options setting popup with any possible options for the language specific (or options for other types of blocks) options. This should allow for changing numbers, input string, or dropdown selection, depending on the type of option. Id like the popup to list all options, with the appropriate input. Make sure to add keyboard navigation in normal mode, h,j,k,l for selecting option by direction, ctrl+n and ctrl+p for selecting from dropdown, i to insert mode on text input, and j,k for incrementing/decrementing number.

* TODO add themes
investigate what themes there and which ones appear to be missing and then
implement them. The more the better.

* TODO todo management

* TODO language modes
  We've added 'ui modes' for languages before (I think just R so far). I'd like
  <leader>rr to run the current document in language modes (and there to be a ui
  mode with a terminal associated to it for running the code in). I'd like to
  add other languages. Let's focus on C/C++ and python. For C/C++, I know I'd
  like a compiler explorer like buffer sidebar and a hex exploration as well.
  I'd also like a terminal that we can run the current file's executable in. For
  python, I'd like object introspection sidebars (like R) and a terminal to run
  the code in and ideally a figure sidebar for matplotlib/seaborn like figures
  to show up in (e.g. that appear when running .show() methods). Can you
  implement these?

* TODO fix buffer sidebar
  The buffer sidebar has some behavior, where, if I click on a buffer, it opens
  that buffer within the current pane (which is the buffer sidebar). Instead,
  I'd like that behavior on double click, except opening to a buffer to its
  right (i.e not inside the buffer sidebar buffer). Then, I'd also like
  click-hold and drag capabilities to drag and drop those buffers into panes
  visible in the windows. We have this capability for the filetree, so maybe we
  can reuse some of that logic there. Can you implement these?

* TODO fix exiting sidebar bug (specifically when exiting todo editor)

* DONE Add a 'gr' binding for 'go run' to run the current file in the language's
  interpreter or compiler. This should be language specific, and should use the
  language's interpreter or compiler (e.g. for R, it should use Rscript to run
  the current file). Can you implement this? In python it should use python, and
  in C/C++ it should use gcc or clang. Can you implement these?
  - Landed as a mep.map_g('r', mep.run_button_run) binding in kBuiltinRunButton
    (main.cpp), i.e. the same per-language run the pane Run button and <leader>rr
    already do: Rscript for .R, python3 for .py, cc/c++ compile-to-temp-then-run
    for .c/.cpp (all overridable per project via the button's Setup popup, or
    globally via mep.opt.run_button_defaults), plus the document pipelines
    (org export, tectonic, rmarkdown/knitr, notebook run-all) and, when a
    language UI mode is open on the tab, sourcing into its live console instead
    of a second process. Verified against a spawned instance over its agent
    socket (:normal gr on a .py file wrote the buffer, opened the tab terminal
    and actually ran the script), with gg/G/bare-r left unaffected.

* TODO Add a 'gd' binding for 'go debug' to start a debugging session for the current file
  in the language's debugger. This should be language specific, and should use
  the language's debugger (e.g. for R, it should use R's built-in debugger to
  start a debugging session for the current file). Can you implement this? In
  python it should use pdb, and in C/C++ it should use gdb or lldb. Can you
  implement these?

* DONE Add a 'gt' binding for 'go test' to run the tests for the current file in the
  language's testing framework. This should be language specific, and should use
  the language's testing framework (e.g. for R, it should use testthat to run
  the tests for the current file). Can you implement this? In python it should
  use unittest or pytest, and in C/C++ it should use googletest or catch2. Can
  you implement these?
  - Landed as kBuiltinLangTest (main.cpp): `gt` (mep.map_g, same shape as
    gd/gh) and :MepLangTest resolve the *test* file covering the current
    buffer, then run it in this tab's popup terminal (mep.tab_terminal_run),
    wrapped in a `(cd <root> && ...)` subshell. Per language: go test -v on
    the file's package, narrowed to the Test/Fuzz/Example functions in a
    _test.go buffer (-run '^(TestA|TestB)$'); pytest when the tree has any
    pytest marker, else `python3 -m unittest discover -p <file>`
    (mep.opt.lang_test_python forces either); testthat::test_file() on
    tests/testthat/test-<name>.R, falling back to test_local() over the
    DESCRIPTION package; for C/C++ the file's own test binary -- the CMake
    target is found via <build>/CMakeFiles/<target>.dir (present whether or
    not it was ever built, Ninja and Make alike), built, then run, with an
    already-built binary or `ctest -R <name>` as fallbacks and
    mep.opt.lang_test_build_dirs as the override; plus cargo test <module>,
    vitest/jest/mocha/npm test, busted, rspec/minitest, Pkg.test() and zig
    test. mep.lang_test_command(path) exposes the resolution without running
    anything. Verified in a spawned instance (xdotool key --window, no focus
    steal): `gt` in cproj/src/pdf_text.cpp resolved pdf_text_test.cpp ->
    mep-pdf-text-test and really executed it, and the emitted go/unittest/
    C++ commands were each run in a shell and passed (mep's own
    src/vterm.cpp -> `cmake --build build/native --target mep-vterm-test &&
    .../mep-vterm-test` -> "vterm_test: all checks passed"). testthat isn't
    installed in this sandbox, so the R command shape is unverified at
    runtime.

* DONE org code block formatting
  I currently can show docs within code blocks, but, I'd also like formatting
  to work (e.g. 'gf').
  - `gf` (and `:MepFormat`) in an org buffer now means "format the code",
    not "format the prose": with the cursor inside a `#+begin_src <lang>`
    block it reformats that block's body, and anywhere else in the file it
    sweeps every block that has a formatter (`:MepOrgFormatBlock` /
    `:MepOrgFormatBlocks` are the same two by name). Added to kBuiltinFormat
    (main.cpp), reusing what org's other per-block tooling already has:
    mep_org_src_block_at finds the block, mep.org_babel_langs' extension
    translates the block's *language name* into the extension key
    mep.format_languages is keyed by (`python` -> `py` -> black, `R` ->
    styler, `cpp` -> clang-format), and a block with no entry at all falls
    back to its own polyglot language server -- textDocument/rangeFormatting
    over just the block's rows, with anything the server wants to change
    outside them refused rather than half-applied. That closes the "no
    formatting bridging" gap polyglot shipped with. A block indented under a
    heading is dedented before the formatter sees it and re-indented after
    (a uniformly indented body is a syntax error to black and gets flattened
    by the others); a body with no shared indent is passed through as
    written. The sweep formats the *last* block first so each application
    can't shift the rows of the blocks still to come, and runs them one at a
    time for the same reason. Nothing is applied if the block was edited or
    the buffer left while the formatter ran, and an already-formatted block
    is left clean rather than rewritten.
  - Verified in a spawned instance (real `gf` keystrokes, own agent socket):
    styler over an indented R block (indentation preserved), clang-format
    over a cpp block, lua-language-server's rangeFormatting over a lua block
    with no formatter entry, the whole-file sweep ("formatted 2 of 3 blocks
    (no formatter for lua blocks)"), plus a Lua-level harness covering the
    failure/staleness/idempotence paths. Two real bugs were caught that way
    and fixed: routing the server's edits through
    mep_polyglot_translate_edits collapsed a whole-body replacement into a
    zero-width insert (the formatted body pasted in *above* the original),
    and asking for a range that reached the `#+end_src` row got back an edit
    one row too long, which the safety check then refused.
  - Also fixed, found while testing this: mep_lsp_diagnostics was a `local`
    in kBuiltinLsp, so kBuiltinOrgPolyglot's own publish handler raised
    "attempt to index a nil value" on every diagnostic and none ever reached
    an org buffer. It is a bare global now, like mep_lsp_server_capabilities
    beside it, and diagnostics render inside code blocks.

* TODO Add C++ language mode
I'd like a C++ language mode that has a terminal for running the current file's
executable, a compiler explorer like sidebar, and a hex exploration like sidebar. Can you implement these?


* DONE gf binding :ARCHIVE:
  Add a 'gf' binding for 'go format' to format the current buffer. This should
  be language specific, and should use the language's formatter (e.g. for R, it
  should use styler::style_file() to format the current file). In python it
  should use black, and in C/C++ it should use clang-format.
  - Landed as kBuiltinFormat (main.cpp): `gf` is a mep.map_g binding (also
    `:MepFormat`) calling mep.format_buffer(), which looks the buffer's
    filetype up in a new mep.format_languages table -- clang-format for
    C/C++ (c/h/cc/cpp/cxx/hh/hpp/hxx/ipp/inl/cu/cuh), black for Python
    (py/pyi), styler::style_file() for R (R/r) -- the same "one filetype ->
    argv table a config can extend" shape kBuiltinRun's mep.run_languages
    already uses. Two formatter shapes are supported: a stdin/stdout filter
    (the default) and an in-place file rewriter (mode = 'file', what styler
    needs), so either way it is the *buffer's* text that gets formatted,
    unsaved edits included, and nothing is written to the real file. '{}'
    in an argv element expands to the buffer's absolute path (filter mode)
    or the temp file (file mode) -- that is what lets clang-format/black
    find the project's .clang-format/pyproject.toml even though they are
    reading stdin. Nothing is applied if the formatter fails, is not
    installed (its own error message), prints nothing, or if the buffer was
    edited/left while it ran; an already-formatted buffer is left clean
    rather than re-written. Filetypes with no entry fall back to
    mep.lsp_format when a server is attached. Verified against a spawned
    instance on its own Xvfb display: real clang-format (honouring a
    fixture .clang-format's IndentWidth), real black, real styler, the gf
    keystroke itself, unsaved-buffer text, missing/failing formatters, and
    the LSP fallback -- which turned up a pre-existing mep.lsp_format bug
    (it applied clangd's mid-line edits as whole-line replacements and
    collapsed the file to a single space), fixed here by routing it through
    mep.lsp_apply_edits_current_buffer.
  - Later change, same entry: R's formatter is now `air format
    --stdin-file-path={}` (filter mode), not styler::style_file() as this
    task asked for. styler has no line width at all -- it fixes spacing,
    indentation and `=` vs `<-` but never breaks a long call across lines,
    at any width, so R was the one language here where gf could not bring
    an over-long line back inside a margin. air is Posit's own tidyverse
    formatter and the only R one with a line width; it still does the
    `=` -> `<-` rewrite styler was wanted for, wraps at 80, and reads its
    margin from the nearest air.toml ([format] line-width) the same way
    clang-format and black read .clang-format/pyproject.toml -- hence the
    switch from mode = 'file' to a plain stdin filter, and the air.toml at
    the repository root. flake.nix gained pkgs.air-formatter; styler had
    never been listed there at all, arriving transitively as one of
    languageserver's dependencies.

* DONE go help binding :ARCHIVE:
  Add a 'gh' binding for 'go help' to open the help for the current function
  under the cursor in the help sidebar. This should be language specific, and
  should use the language's help system (e.g. for R, it should use help() to
  open the help for the current function). Can you implement this? In python it
  should use pydoc, and in C/C++ it should use man pages. Can you implement
  these?
  - `gh` is now a language dispatch (kBuiltinGoHelp, main.cpp) instead of the
    R-only binding the r-help-format branch left behind: each language UI mode
    registers a provider (mep.help_register_provider) that gates on its own
    per-tab state and returns true once it has handled the lookup, and when
    none claims the buffer the chain ends at mep.lsp_hover -- the same popup K
    already gave. Also :MepGoHelp and <leader>hs (mep.map_g takes no
    description, so the leader spelling is what makes it discoverable in the
    keymaps picker).
  - R: unchanged behavior (help('sym') / help('sym', package = 'pkg') into the
    Help tab), now registered as the 'r' provider and sharing the new
    mep.help_symbol_at_cursor scanner.
  - Python: mep_help('sym') into the console, a new helper in the session's
    init script that evaluates the name in the console's own namespace first
    (so `sorted` and a dotted `os.path.join` both resolve) and falls back to
    handing pydoc the bare string; it flows through the same overridden help()
    -> pydoc -> help.txt -> poll path as a typed help(x), and the Help tab is
    revealed immediately.
  - C/C++: the mode grew a Help tab (Assembly/Build/Help top-right now), and
    gh runs `man 3 sym || man 2 sym || man sym` (piped through `col -bx` when
    installed) into it, MANWIDTH set to the tab's own width; the identifier
    scan takes ':' too, so std::vector stays one topic.
  - Verified on a spawned instance on its own Xvfb display, driving real `gh`
    keystrokes (xdotool --window) and the agent socket: printf(3)/strcpy(3)
    into the C mode's Help tab, "No man page for buf." for a local variable,
    sorted + os.path.join in the Python mode, stats::lm -> help('lm', package
    = 'stats') in the R mode, and "No LSP attached" for gh in a plain buffer
    with no mode open.

* DONE r help format :ARCHIVE:

  The formatting of help documentation in R help sidebar looks off. It looks
  good when in the popup though when I press 'K' over a function name in an R
  document. Can you fix the sidebar formatting to match that of the help popup?
  Also, I'd like gh to be added as a keybinding to 'go to help' that, when in ui mode
  in R will open the help for that function in the help sidebar (and move
  visibility to the help pane if not an actively viewable pane). Can you implement these?

* DONE r gh binding :ARCHIVE:
  In R, I'd like 'gh' when in ui mode and over a function name to send that
  function the the help (and appear in the help sidebar). Can you implement this?
  - Already landed with the r-help-format branch (kBuiltinLanguageUiR,
    main.cpp): `gh` is a mep.map_g binding calling mep.r_ui_help_at_cursor(),
    which, while the tab's R UI mode is open, reads the R identifier under
    the cursor ([%w_.], so spline.des stays whole; a `pkg::sym` prefix
    becomes package = 'pkg'), writes help('sym') into the session console
    (same overridden help() -> help.txt -> poll path as a typed ?topic, choice
    list for an ambiguous topic included) and mep.jump_to_buffer()s the Help
    tab so it is revealed even when hidden behind Data/Objects/... or after a
    repeat lookup of the same topic. Outside an R UI mode it falls back to
    mep.lsp_hover(). Verified in this branch's fresh build against a spawned
    instance over its agent socket (:normal gh, so no X focus needed): plot
    (choice list), lines, spline.des, stats::lm -> help('lm', package =
    'stats'), and the "no R symbol under the cursor" warning on a blank line.

* DONE r help: run lines + vim modal editing :ARCHIVE:
  I'd like to be able to run lines of code from the R help. On a line, I should
  be able to run the line of code with mod+<return>. Also, I'd like to be able to
  use vim modal editing here too, so I can see the cursor and enter visual,
  visual-block mode, etc.
  - The R UI mode's Help tab is now a real text buffer (mep.pane_open of a
    session-dir file named `Help`, so the tab title stays "Help"; same idiom
    as the editable file tree) instead of a sidebar widget list, so it has a
    real cursor and every Normal/Visual/Visual-block/search/yank motion for
    free. It is registered as a mep.termsend source aimed at the session's
    console, so the stock mod1+Enter (kBuiltinTermSend) runs the line under
    the cursor in R and steps down, or sends the Visual selection. Rd2txt
    text (width 500) is laid out by mep_r_ui_help_lines (main.cpp): prose
    word-wrapped to the pane's real width with hanging indents, Usage:/
    Examples: lines verbatim so mod1+Enter runs exactly what R printed,
    title/section highlights via buffer decorations, header collapsed to
    `topic  {pkg}`. The pane width comes from a new mep.buffer_text_cols(id)
    (Pane::text_cols, set by DrawPane; Editor::TextColsForBuffer), and the
    poll loop re-wraps whenever it changes. An ambiguous topic renders its
    candidates as literal `help('topic', package = 'pkg')` lines, so picking
    one is just mod1+Enter on it (.mep_help_pick is gone). gh still reveals
    the tab and puts the cursor at its top on new content; closing the mode
    deletes the buffer. Verified on a spawned instance over its agent socket:
    gh -> lines/plot (choice list), running the base choice with the
    termsend line sender, resize re-wrap, close cleanup.

* DONE fix pdf rendering :ARCHIVE:
  While pdf rendering works, there are some pdfs it fails spectacularly on, in
  particular, ones with math. I think this used to work when we were using
  pdfium to do this. I'd like to fix it eventually, but in the short run, would
  like it to just work. Can you fix the backend so that we can swap out our
  inhouse pdf rendering engine with the pdfium rendering engin. Maybe we need a
  bridge pattern or something so these can be swapped in and out easily.


* DONE git sidebar binding :ARCHIVE:
  I'd like in the status tab in the git sidebar, for pressing P to push.
  Can you implement this?
* TODO fix exiting sidebar bug (specifically when exiting todo editor)
  :LOGBOOK:
  CLOCK: [2026-09-19 Sat 09:43]--[2026-09-19 Sat 09:46] =>  0:03
  CLOCK: [2026-09-19 Sat 09:43]--[2026-09-19 Sat 09:43] =>  0:00
  :END:
* DONE add jupyter notebook mode :ARCHIVE:
  I'd like the ability to edit jupiter notebook files, much like the interface
  of standard web-based jupyter interface. CAn you add these capabilities? Add a
  jupyter notebook file in the in the examples folder to test things out
  - Opening a `.ipynb` (Editor::LoadFile -> OpenNotebookInPlace) shows it as
    an ordinary editable text buffer in the "percent" cell format (`# %%` /
    `# %% [markdown]` markers, raw markdown bodies -- src/notebook_doc.h's
    top comment), so every vim motion/edit/undo/search works on cell
    sources; outputs, execution counts, ids and metadata live in a
    NotebookSession side model (Editor::notebooks_) re-attached to the text
    by cell index/source (SyncNotebookFromLines) and written back as
    nbformat 4 JSON on `:w` (SaveBuffer's notebook arm, Jupyter's own
    1-space indent so diffs stay clean). Cell chrome (header band with
    "In [n]:"/Markdown label + a clickable Run chip, Accent left bar on the
    cursor's cell) and each code cell's outputs (stdout/stderr/result text,
    red tracebacks, inline PNG figures) are drawn by DrawPane's notebook
    branch as extra visual slots under the cell's last row --
    Editor::NotebookTrailingSlots, the org-inline-image "one row, N slots"
    mechanism, kept in agreement across the draw loop, its RowSlot cursor
    lookup and UpdateScrollForPane.
  - Execution: one persistent `python3` process per notebook (JobManager,
    stdin/stdout JSON lines -- NotebookKernelScript() in notebook_doc.cpp:
    shared namespace, IPython-style last-expression display with `;`
    suppression, tracebacks trimmed to the cell's frames, streaming stdout,
    KeyboardInterrupt via a new JobManager::Interrupt/SIGINT, and an inline
    matplotlib backend that ships every open figure as PNG at the end of a
    cell or on plt.show()). No jupyter_client/ZMQ dependency; the
    interpreter is `mep.opt.notebook_python` (default python3; flake.nix's
    devShell python now carries matplotlib for the figure cell).
  - Keys: Enter / Ctrl+Enter run the cell under the cursor, Shift+Enter
    runs and moves to the next cell (creating one at the end), mod1+Enter
    runs and inserts below -- in Normal and Insert mode, C++-side since
    mep.map can't bind Enter. `<leader>j` group (kBuiltinNotebook): jr run,
    jn run+next, jo run+insert, jA run all, ja/jb insert above/below, jd
    delete, jm/jy to markdown/code, jk/jj move up/down, jc/jC clear cell/all
    outputs, ji interrupt, j0 restart kernel; `]j`/`[j` step cells; `:Notebook*`
    commands for all of them plus `:NotebookNew [path]`; `<leader>rr` runs
    all cells. Cell bodies are syntax-highlighted per cell (python /
    markdown) by kBuiltinSyntax's notebook branch.
  - Lua surface for scripts/agents: mep.notebook_is_buffer/run_cell/run_all/
    run_and_advance/run_and_insert/interrupt/restart_kernel/clear_outputs/
    insert_cell/delete_cell/set_cell_type/move_cell/cell_index/cell_count/
    goto_cell/set_python/status/cell_outputs.
  - Tests: src/notebook_doc_test.cpp (mep-notebook-doc-test, in `just test`)
    covers parse/serialize round trips, the text view, cell re-matching after
    inserts/deletes, output display/slot math and the wire protocol.
    examples/notebook_example.ipynb (generated through the real kernel, with
    a stored matplotlib figure and a traceback) is the fixture to try it on.
* DONE Filetree image viewer mode :ARCHIVE:
  - `:e`/`mep.open` on a directory now opens the file tree in the current
    pane (Editor::LoadFile's new directory branch -> mep.set_on_directory_open
    -> kBuiltinFileTree's mep.tree_open_in_pane, mep.buffer_switch) instead
    of silently creating an empty buffer; `<leader>ff`/:MepFileTree's own
    split-left mep.tree_open is unchanged.
  - Shift+I on the tree buffer toggles an image viewer showing the first
    image (alphabetically) in the tree's root directory, reusing the
    existing "<"/">" nav-header and h/l-stepping machinery (ImageSession::
    nav_prev_ref/nav_next_ref, built for kBuiltinLanguageUiR's Plot pane)
    rather than new UI; stepping clamps at the first/last image. A new
    ImageSession::return_ref + mep.image_set_return pairs with a new
    Editor::SetBufferOnImageToggle/mep.buffer_set_on_image_toggle hook on
    the tree buffer so Shift+I toggles back to the tree from the image.
  - Verified end-to-end against a spawned native instance (raw agent-socket
    RPC + xdotool-focused key injection): directory open, first-image open,
    h/l clamp at both ends, Shift+I round-trip back to the tree, empty-
    directory notification, and no regression to plain file opens or the
    split-left tree toggle.

* DONE ai tabbar notification :ARCHIVE:
  Add a notification to the tabbar workspace tab when finishing tasks or being
  asked to interact. I'd like the workspace tab background to be red (if not an
  active workspace)
  - An inactive workspace whose resident agent reports "awaiting_input" or
    "done" (Editor::WorkspaceNeedsAttention, src/editor.cpp) now draws its
    tab with a red background (new "WorkspaceAlertBg" highlight group,
    src/editor.cpp's BuildHighlightGroups) and red text in the tabbar
    (src/main.cpp's DrawTabBar). The join walks Editor::Participants(),
    matching each agent's terminal buffer (falling back to its cursor
    location) to a workspace via Editor::BufferWorkspaceId -- the same
    pattern the Lua AI-agents sidebar already used. The collapsed "+N
    hidden" indicator also turns red if any hidden workspace needs
    attention, and the tab's tooltip gets an "-- agent needs you" suffix.
    Verified live: spawned a second detached `mep` instance, created a
    second workspace, identified a synthetic agent against a buffer in it,
    set its status to "awaiting_input", switched away, and confirmed the
    inactive tab rendered red via screenshot.

* DONE Add a direnv widget to the bottom bar :ARCHIVE:
  I'd like a direnv widget in the bottom bar that shows the current direnv
  status (active/inactive) and allows me to toggle it on/off with a click.
  (note, it should activate by default when opening a project). Place it 
  on the left side of the pomodoro timer, but on the right side of the line and
  column indicator.

* DONE Fix folded style for org mode files :ARCHIVE:
  The fold style for org mode files doesnt look great. Can you make it look
  better? I think it should have a different (highlighted) background color,
  and should have a ... at the end of the line. Otherwise, it looks like a
  normal line. Can you make it look better?

* DONE Add tabs, nested within workspaces, nested within projects feature :ARCHIVE:
 - Need to have new git worktrees per workspace
 - Need to have buffers scoped to workspaces
 - Need to remove mode from top tab bar
 - Need to add project to left side of tab bar formatted like [project_name] and
 tabs formatted like [ws1] [ws2] highlighted by active workspace
 - Implemented 2026-09-03 across all 12 phases of WORKSPACES_PLAN.md (see its
   Status section for what was verified how, and what still needs a GPU-capable
   display to confirm by eye).
* DONE Add reorder todo feature with ctrl+j,k :ARCHIVE:
  :LOGBOOK:
  CLOCK: [2026-09-05 Sat 22:03]--[2026-09-05 Sat 22:12] =>  0:09
  :END:
* DONE Add better styling to mode in bottom bar and remove filename :ARCHIVE:
* DONE add image editor :ARCHIVE:
* DONE add latex run button :ARCHIVE:
* DONE add orgmode run button :ARCHIVE:
  - Run button now shows for .org files; picks the target format from a
    `#+EXPORT:` header keyword (html/pdf/odt/markdown/ascii, default html),
    runs the matching mep.org_export_* exporter, and opens/refreshes the
    result in a vertical split using the appropriate viewer (browser-style
    HTML pane, PDF viewer, or office WYSIWYG pane), reusing an already-open
    split instead of stacking up new ones.
  - Added mep.pdf_reload/mep.office_reload (editor.cpp/lua_env.cpp) so an
    already-open PDF/ODT preview can be refreshed in place, mirroring the
    existing mep.html_reload.
* DONE bind run button to <leader>rr :ARCHIVE:
* DONE add colors to tabbar buttons :ARCHIVE:
* DONE decrease size of tabbar tab button icons (i.e. just for tab, new tab, delete tab) :ARCHIVE:
* DONE remove ? mark and swap with [+] for adding new workspace :ARCHIVE:
* DONE add [-] for delete active workspace :ARCHIVE:
* DONE add zen mode :ARCHIVE:
  :LOGBOOK:
  CLOCK: [2026-09-05 Sat 23:14]--[2026-09-05 Sat 23:20] =>  0:06
  :END:
* DONE add pomodoro timer widget :ARCHIVE:
* DONE make todo list have line wrapping :ARCHIVE:
* DONE Fix cursor bug :ARCHIVE:
  There are certain times that an action will occur twice. I've noticed
  sometimes in normal files the cursor will move two spaces when pressing j or
  k. Other times, I'll see this behavior in the terminal, where deleting a
  character will delete two. The behavior does seem inconsistent though. Can you
  investigate and fix?

* DONE Add archive behavior :ARCHIVE:
  On archiving a todo item, i'd like its todo moved to the bottom of the todo
  document. Can you implement this behavior

* DONE Implement gaps :ARCHIVE:
  The old sidebars had a nice few pixel gap between them and the normal panes.
  I like that, but, I think I want them in between all normal panes (i.e.
  everywhere). Can you implement gaps betwen panes and set the gap width to be
  5 pixels?

* DONE Fix git tab rendering bug :ARCHIVE:
  The tabs in the git sidebar (the new one) bleed over into the neighboring pane
  when the sidebar has a narrow enough width. This seems like a bug. Can you
  investigate and fix?
* DONE Add a start agent binding to the todo sidebar :ARCHIVE:
  I'd like a start agent binding for the todo sidebar activated with 'L'. It
  should take the todo, and its text, prompt the user for a workspace (branch)
  name, and create a new workspace and start up a project ai agent terminal
  inside it. If a workspace is currently active with an agent running, instead of a space
  or an x in the todo list, can you place a robot icon indicating that an agent is currently
  working on this todo? Can you implement this? I'd like this same behavior to work in a TODO.org file
  with the binding 'gL' in normal mode (i.e. on the current todod where the cursor is). 
* DONE Fix agents to be local to a mep instance. :ARCHIVE:

  Currently, it seems like while working on MEP, I can't have agents running
  because it might need to close down MEP (and hence the agents that are
  running) during the testing process. Instead, I'd like it to reliably be able
  to spawn new instances of MEP and have instructions to not close the
  containing MEP instance. I think this might require allowing multiple mcp
  servers running and tying them to a mep instance and also modifying the agent
  terminal instructions to be informed of which MEP instance it is running from
  and to be sure not to close it if working on MEP. I'd also like ai agents to
  be sure to only use the MEP api when interacting with any MEP instance and
  not the x11 server, unless specifically working on testing mouse/keyboard
  input. Can you implement these? This way, I can have multiple agents running
  and working on MEP at the same time, and they won't interfere with each
  other.

  - Each running mep instance already binds its own agent-control socket
    (`~/.local/share/mep/agent-sockets/<pid>.sock`) and exports it as
    `MEP_AGENT_SOCKET` into any terminal it spawns, so a `mep-mcp` process
    started from inside an AI terminal always drives the instance it was
    launched from, never a sibling window -- multiple agents/mep windows
    already don't contend for or cross-talk over a shared server.
  - What was missing was telling the agent this, and warning it about the
    real failure mode: an agent developing mep itself runs as a child (in
    the process group) of the very instance it would want to rebuild and
    test, so quitting/restarting that instance to pick up a change kills
    the agent's own session along with it (`JobManager::ShutdownAll` SIGTERMs
    the whole process group on shutdown). Added a new "developing mep
    itself" section to `mep.opt.ai_terminal_instructions`
    (`kBuiltinAiTerminal`, `src/main.cpp`) and `MEP_AGENT_API.md` telling
    the agent to identify its own containing pid (`mep_session_info`'s
    `pid`, or its own `MEP_AGENT_SOCKET`'s `<pid>.sock` filename), never
    close/kill/restart it, and instead launch a separate `setsid`-detached
    instance to test changes against and drive it via its own socket.
  - Also strengthened the UI-automation section to say explicitly: prefer
    the structured `mep_*` editor-state tools for any mep instance, and
    reach for `mep_mouse_*`/`mep_key_press`/`mep_screenshot` only when the
    task is specifically about testing mouse/keyboard/rendering input.

* DONE Org file robot animation :ARCHIVE:
  When using the agents to complete a todo item in an org mode document (i.e.
  with gL), I'd like it to activate a little animation at the end of the todo
  item. Here, I'd like a robot icon, with a ... that animates . and then .. and
  then ... and back to . and repeat. Can you implement this behavior? I'd like
  the same animation in the todo sidebar (currently just a robot icon). I'd like
  this to be triggered in both, whether starting in the sidebar or in the
  TODO.org file.

  - Added `Decoration::virt_robot_icon` (editor.h) so a buffer decoration's
    `virt_text` can be prefixed with the same hand-drawn `DrawRobotIcon`
    glyph the Todo sidebar already overlays, exposed through
    `mep.deco_add`/`mep.buffer_deco_add`'s `virt_robot_icon` option
    (lua_env.cpp) and drawn in `DrawPane`'s decoration loop (main.cpp)
    just before `virt_text`, shifting it right by the icon's own width.
  - Added a shared `mep_todo_agent_dots()` Lua helper (kBuiltinActivityBar,
    main.cpp) cycling ". -> .. -> ... -> .. -> repeat" off `mep.now()`,
    used by both the Todo sidebar's row text (after the existing robot
    mark) and a new `mep_activity_todo_decorate_buffer()` that finds
    TODO.org's own open buffer (by filename, across workspaces) and adds
    a `virt_text_eol` + `virt_robot_icon` decoration on each headline with
    a live agent -- so the animation shows up in-buffer too, wherever the
    agent was started from ('L' in the sidebar or 'gL' on the headline).
  - `mep_activity_todo_key` now folds the dots' own phase in whenever any
    row is agent-live, and the sidebar's poll interval tightened from
    0.5s to 0.15s (still just a cheap string compare when nothing
    changed) so the animation reads as smooth rather than jumpy; a
    separate 0.15s poll (gated on `mep.activity_todo_agents` being
    non-empty) drives the in-buffer decoration independently of sidebar
    visibility.
  - Verified by spawning a separate `setsid`-detached mep instance
    (native-GUI build, real X11/GPU in this sandbox) and driving it over
    its own raw agent-control socket: injected a `virt_robot_icon`
    decoration on a headline and a matching sidebar row directly, and
    confirmed both render the robot glyph followed by the animated dots
    with correct spacing, across all four dot-count states.
* DONE filetree button fix :ARCHIVE:
The filetree button in the tabbar doesnt toggle the filetree (it just opens it). Can you fix this?

* DONE Fix tabbar buttons :ARCHIVE:
  The tabbars associated to the right tabbar use the old sidebar style sidebars.
  I'd like to replace them with the buffer style tabbars. The ones on the left
  side work. However, they don't open them as a vertical split towards the
  bottom of the left side. Instead, they open them side-by-side even though it
  works when I use the bindings (e.g. <leader>ff, <leader>bB, and <leader>gg).
  Can you investigate and fix this so that the tabbar buttons have the same
  behavior.

* DONE Fix performance issues :ARCHIVE:
  Root cause: mep.on_frame(fn) had no way to unregister a hook -- every call
  permanently registered a per-frame Lua call. mep.activity_todo_start_agent's
  own wait-state hooks (used by both 'L' in the Todo sidebar and 'gL' on a
  TODO.org headline) had a `done`-style flag but the hook body itself kept
  being invoked every single frame forever after finishing -- so each agent
  start left two more dead closures in the per-frame dispatch permanently,
  and repeating it over a session degraded the whole editor's frame
  throughput (including plain keyboard input like j/k), exactly matching
  "clunky, needs two keypresses, fast without an agent running."
  - LuaEnv::RunFrameHooks/CallFrameHookRef (lua_env.cpp) now let a hook
    signal it's done by returning a truthy value, dropping it from
    frame_hook_refs_ instead of calling it forever; mep.activity_todo_
    start_agent's two wait-state hooks (kBuiltinActivityBar, main.cpp) were
    converted to this shape, including the "terminal went away mid-wait"
    abort path so they can never re-leak.
  - Also fixed: mep.activity_todo_agents never pruned a finished agent's
    entry, so every todo an agent had ever touched kept paying a live-agent
    scan (workspace_list + participants + ai_terminals) on every 0.15s
    sidebar/decorator poll for the rest of the session -- mep_todo_agent_
    live() now drops an entry once an agent actually seen alive goes away.
  - New mep-lua-frame-hook-bench (bench_results/history.jsonl, `just bench`)
    demonstrates the shape: permanent no-op hooks get linearly more
    expensive per frame as they accumulate, while one-shot self-removing
    hooks cost ~nothing on the very next pass after they fire.
  - Verified: `just bench` builds clean and runs (crdt/buffer/frame-hook
    benchmarks all recorded); re-read every mep.on_frame hook in main.cpp
    (20 call sites) to confirm no other one-shot-disguised-as-permanent
    leak of this shape exists elsewhere.

* DONE Add DAP debugging capabilities. :ARCHIVE:
  Add DAP debugging capabilities. Start with R, Python, and C/C++. I also want a
  debugging ui, triggered with <leader>du that has all of the symbols and such
  in sidebars and a console, etc. Buttons for going stepping into, out of, etc.
  Use examples in the example folder to test out and the flake.nix for adding
  dependencies to the shell. I'd like <leader>dd to continue/start debugging. I'd
  like <leader>db to toggle a break point (visible in the side line number gutter),
  <leader>di to step into, <leader>do to step out of, and a couple others that might make
  sense if you can think of them. Can you implement these?

  Also folds in the separate "Fix dap debug ui" todo -- this same change is
  what actually builds the debug UI for the first time.

  - The pre-existing DAP client (kBuiltinDap) only ever sent breakpoint
    toggles and one-line continue/step requests, with no keybindings, no
    reaction to the `stopped` event beyond a toast, and -- a real protocol
    gap -- no way to answer a server-initiated *request* (e.g.
    runInTerminal), which left such adapters hanging forever. Fixed in
    lua_env.cpp: DispatchLspMessage now answers server requests
    (mep.lsp_on_request), and kBuiltinDap's `stopped` handler now runs a
    real threads -> stackTrace -> scopes -> variables chain, jumps the
    cursor to the paused line, and drops a green "▶" gutter arrow
    (namespace `dap_current_line`, kept separate from the breakpoint
    dot's own namespace so toggling a breakpoint mid-session can't wipe
    it).
  - New `kBuiltinDebugUi` (loaded right after kBuiltinDap, mirroring this
    file's existing kBuiltinRun/kBuiltinRunButton mechanics-vs-UI split):
    a "Debug" sidebar (Call Stack / Variables / Breakpoints sections) and
    a Debug Console pane, plus every requested keybinding under a new `d`
    leader group -- `<leader>du` (toggle the sidebar+console), `dd`
    (start/continue, resolving the language from the current file's
    extension), `db` (toggle breakpoint), `dn`/`di`/`do` (step over/into/
    out -- `dn` since `do` was already spoken for), `dt` (terminate),
    `dr` (restart), `dc` (clear all breakpoints in the file), and `dv`
    (evaluate the expression under the cursor).
  - R has no DAP adapter that speaks stdio the way lldb-dap/debugpy do --
    the real one, vscDebugger, opens a plain TCP socket instead. Built a
    real TCP transport for this rather than just documenting the gap:
    new `TcpJsonRpcManager`/`TcpConnection` (src/tcp_client.h/.cpp,
    mirrors job.h's threading/PollAll shape) plus `mep.lsp_connect`
    (lua_env.cpp). `mep.dap_start('r')` spawns R itself via
    `mep.term_start` (R's own stdout isn't LSP-framed) running
    vscDebugger's real (undocumented/unexported, confirmed against its
    source) `.vsc.listenForDAP()`, then retries `mep.lsp_connect` for a
    few seconds until R's socket comes up.
  - Along the way, live-testing against a *real* lldb-dap surfaced (and
    fixed) two protocol bugs this client had never had a live adapter to
    catch before: (1) DAP is NOT JSON-RPC 2.0 -- only the Content-Length
    header framing is shared; the JSON body is `{seq, type, command,
    arguments}` / `{seq, type: "response", request_seq, success, body}` /
    `{event, body}`, nothing like `{jsonrpc, id, method, params}`. Added
    a `dap_mode` flag to the LSP/DAP client (lua_env.cpp) with its own
    DispatchDapMessage, so this client now speaks real DAP instead of a
    shape no adapter actually accepts. (2) Debug builds compiled for the
    new C/C++/Rust launch path came out fully optimized regardless of the
    `-g` flag -- this toolchain's `_FORTIFY_SOURCE` hardening silently
    adds `-O2` when no `-O` is given, which inlines away small functions
    (examples/cpp_example.cpp's `distanceTo` had zero line-table entries
    at -O2, so lldb reported every breakpoint inside it as an unresolved
    "pending" location and never stopped). Fixed by always appending
    `-O0` after `-g`.
  - flake.nix: added `pkgs.lldb` (ships `lldb-dap`), `debugpy` to the
    existing Python package set, and a hand-packaged `vscDebuggerR`
    (`rPackages.buildRPackage` + `fetchFromGitHub`, since vscDebugger
    isn't on CRAN) added to the R package set.
  - Verified end-to-end against real spawned adapters (a detached mep
    instance driven over its own raw agent-control socket, per
    MEP_AGENT_API.md): C++ (lldb-dap) and Python (debugpy) both go
    start -> stop at entry -> continue -> hit a real breakpoint -> show
    correct call stack/locals -> step over -> terminate, exactly as
    intended. R (vscDebugger) verified up through a real TCP connect,
    initialize, launch, setBreakpoints, configurationDone, and a genuine
    `stopped`/breakpoint event -- but the session then ended before this
    client's follow-up stackTrace/variables requests could complete;
    vscDebugger's `debugMode = 'file'` runs the whole script from inside
    `configurationDoneRequest` and stops listening right after, per its
    own source, so sustaining an interactive pause for `file` mode may
    need a different launch shape than this change had time to track
    down. The R *transport* (the actual ask, given no stdio adapter
    exists) is solid and reusable; R *session* behavior past the first
    breakpoint is the one open item, noted in kBuiltinDap's own comment
    next to `mep.dap_adapters.r`.

* DONE Native file open popup :ARCHIVE:
  Add platform native file-open popup (instead of :e , the current behavior) when using File->Open

* DONE remove flamboozled :ARCHIVE:
  Removed the stray "flamboozled" line from the top of README.org.

* DONE Remove ai tabbar notification (agent-attention highlight) :ARCHIVE:
  Reverted the "ai tabbar notification" feature: an inactive workspace tab
  no longer turns red when its resident agent reports "awaiting_input" or
  "done". Suspected as a performance culprit -- Editor::WorkspaceNeedsAttention
  called Editor::Participants() (which allocates a fresh vector by walking
  collaboration peers, AI agents, and local participants) once per workspace,
  every single frame in main.cpp's DrawTabBar.
  - Removed Editor::WorkspaceNeedsAttention (editor.h/editor.cpp) and the
    "WorkspaceAlertBg" highlight group (editor.cpp's BuildHighlightGroups).
  - Removed the needs_attention field and all red-tab/red-tooltip/red-"+N
    hidden" logic from DrawTabBar (main.cpp) -- inactive tabs render exactly
    as they did before that feature, plain "WorkspaceInactive".
  - Verified `just build-native` still builds clean.

* DONE add simple todo :ARCHIVE:
  Added the word "flummoxed" to the top of README.org.
* DONE Add language specific, configurable run button to active pane bar :ARCHIVE:
* DONE quickjump capability :ARCHIVE:
  I'd like a quickjump capability. In particular, when pressing 's' in normal
  mode, I'd like to then be able to start typing, and any non-matches to the
  thing I'm typing to appear more opaque and for the matches to appear less
  opaque with a hint I can type (just a letter or two ideally) to jump to that
  matching location, or when I finish typing and there are no matches to jump to
  it. Can you implement this feature?
  - Done as a new Editor::Mode::QuickJump (editor.cpp, next to the older
    HintChar/HintLabel pair) bound to `s` by kBuiltinQuickJump (main.cpp)
    via mep.quick_jump(), so it can be rebound with a plain mep.map. Every
    keystroke rescans the visible rows of every text pane in the tab (the
    active one first, then the rest in split order; a label in another
    pane focuses it before moving its cursor; smart-case): DrawPane
    washes each pane's text area with translucent NormalBg, repaints each match
    at full strength on an IncSearch backdrop, and draws a one-key label
    right after it. Labels are drawn from the home-row-first pool minus
    every character that would extend the query at some match, so a key
    is never ambiguous (label -> jump, else -> narrow); nearest match
    gets the first label. A query that narrows to one match jumps at
    once; Enter jumps to the nearest, Backspace edits, Esc cancels; the
    command line echoes "jump: <query>" (+ "(no matches)"). Landing
    records the jumplist so `` / Ctrl-O return. README "Keys" documents
    it. Verified against a spawned instance over its agent socket
    (ui.key_press/type_text + screenshots): label jump, unique-match
    auto-jump, Esc/Enter-with-no-match leaving the cursor put, Backspace.
    Cross-pane: verified over the agent socket alone via the scriptable
    surface added for it -- mep.quick_jump(query?), mep.quick_jump_matches()
    (pane_id/row/col/label), mep.quick_jump_pick(label) -- so no synthetic
    keyboard input is needed: labels span both panes, picking another
    pane's label focuses it, a unique match elsewhere auto-jumps, and a
    terminal pane is skipped.
