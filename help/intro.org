#+TITLE: mep Help
#+HTML_HEAD: <meta name="help-section" content="Getting started">
#+HTML_HEAD: <meta name="help-order" content="10">
#+HTML_HEAD: <link rel="stylesheet" href="help.css">

* Welcome

mep is a modal editor. It opens with an empty buffer and expects you to press
=i= before you can type; everything else follows from that one idea.

It is also, by now, a good deal more than a text editor: it renders and edits
Org and Markdown, PDFs, Word documents, spreadsheets, Jupyter notebooks,
images, 3D models and web pages, each in an ordinary pane, and it has its own
browser, its own Lua scripting layer, and its own agent protocol.

This is the manual. The sidebar on the left groups every page by topic; click
a page to read it, or press =f= to label every link on screen and jump to one
by typing its letter.

* New here

Read these in order. Twenty minutes gets you from nothing to comfortable.

- [[file:first-steps.html][Your first ten minutes]] -- modes, moving, editing, saving, quitting.
- [[file:dashboard.html][The start screen]] -- what you are looking at before you open anything.
- [[file:installing.html][Installing and running]] -- building mep, the command line, =init.lua=.
- [[file:vim-differences.html][What is and isn't Vim]] -- read this early if you come from Vim.

* Everyday use

- [[file:editing.html][Editing basics]] -- buffers, panes and the editing model.
- [[file:shortcuts.html][Keyboard shortcuts]] -- the keys you will reach for most.

* Finding your way around

Three things will answer almost any "how do I..." question without leaving the
editor:

| Key         | What it gives you                                            |
|-------------+--------------------------------------------------------------|
| =<Space>=   | the which-key popup: every leader binding, grouped and named |
| =<leader>hk= | a searchable list of every key binding currently in effect   |
| =:=         | the command line, with completion over every command name    |

Press =<leader>hh= (or run =:MepHelp=) to come back to this page at any time.

* Contributing to this manual

- [[file:writing-help.html][Writing help pages]] -- the page format, and how to add one.
